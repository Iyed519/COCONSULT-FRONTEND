/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Quiz } from '../../models/quiz';

export interface GetQuizzesByUpdatedAtBetween$Params {
  start: string;
  end: string;
}

export function getQuizzesByUpdatedAtBetween(http: HttpClient, rootUrl: string, params: GetQuizzesByUpdatedAtBetween$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Quiz>>> {
  const rb = new RequestBuilder(rootUrl, getQuizzesByUpdatedAtBetween.PATH, 'get');
  if (params) {
    rb.path('start', params.start, {});
    rb.path('end', params.end, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Quiz>>;
    })
  );
}

getQuizzesByUpdatedAtBetween.PATH = '/quiz/by-updated-at-between/{start}/{end}';
